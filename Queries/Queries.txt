1. 
PREFIX ex: <http://example.org/ns#>
PREFIX dbo: <http://dbpedia.org/ontology/>
PREFIX dbr: <http://dbpedia.org/resource/>

SELECT ?course WHERE {
  dbr:Concordia_University dbo:offersCourse ?course .
}

2.
PREFIX ex: <http://example.org/vocab/>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>

SELECT ?course ?courseName
WHERE {
  ?topic ex:topicName "CNN"^^xsd:string .
  ?topic ex:isTopicOfCourse ?course .
  ?course ex:description ?courseName .
}


3. 
PREFIX ex: <http://example.org/vocab/>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?topicName
WHERE {
  ?topic ex:isTopicOfCourse <http://example.org/vocab/course/40355> ;
         ex:isTopicOfLecture <http://example.org/vocab/lecture/7> ;
         ex:topicName ?topicName .
}

4.
PREFIX ex: <http://example.org/vocab/>
PREFIX dbo: <http://dbpedia.org/ontology/>

SELECT ?course ?subject ?number
WHERE {
  ?university dbo:offersCourse ?course .
  ?course a ex:Course ;
          ex:subject ?subject ;
          ex:number ?number .
  FILTER(?subject = "COMP" || ?subject = "SOEN")
}

5.
PREFIX ex: <http://example.org/vocab/>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>

SELECT DISTINCT ?lectureName
WHERE {
  # Select the specific course by subject and number
  ?course a ex:Course;
          ex:subject "COMP"^^xsd:string;
          ex:number "6741"^^xsd:string.

  # Find topics that are part of this course
  ?topic a ex:Topic;
         ex:isTopicOfCourse ?course;
         ex:topicName "Amazon"^^xsd:string.  # Adjust topic name as necessary

  # Find lectures that include this topic
  ?topic ex:isTopicOfLecture ?lecture.
  
  # Get the names of these lectures
  ?lecture ex:lectureName ?lectureName.
}

6.
PREFIX ex: <http://example.org/vocab/>

SELECT ?credits
WHERE {
  ?course a ex:Course ;
		ex:subject "COMP" ;
          ex:number "6651" ;
          ex:credits ?credits .
}

7.
PREFIX ex: <http://example.org/vocab/>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?seeAlso
WHERE {
  ?lecture a ex:Lecture ;
           ex:lectureOfCourse ?course ;
           rdfs:seeAlso ?seeAlso .
  ?course ex:number "6721" ;
          ex:subject "COMP" .
}

8.
PREFIX ex: <http://example.org/vocab/>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>

SELECT ?topicName ?topicLink ?materialType
WHERE {
  ?course a ex:Course;
          ex:subject "COMP"^^xsd:string;  
          ex:number "6741"^^xsd:string.   

  ?lecture a ex:Lecture;
           ex:lectureOfCourse ?course;
           BIND (IRI(CONCAT("http://example.org/vocab/lecture/", "7")) AS ?specificLecture)

  ?topic ex:isTopicOfLecture ?specificLecture;
         a ex:Topic;
         ex:topicName ?topicName;
         ex:materialType ?materialType. 

  BIND (IRI(?topic) AS ?topicLink)
}


9.
PREFIX ex: <http://example.org/vocab/>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>

SELECT ?lectureNumber ?materialType
WHERE {
  # Identifying the specific course by subject and number
  ?course a ex:Course;
          ex:subject "COMP"^^xsd:string;  # Specify the course subject
          ex:number "6741"^^xsd:string.   # Specify the course number

  # Finding topics associated with this course and matching a specified topic name
  ?topic a ex:Topic;
         ex:topicName "Amazon"^^xsd:string;  # Specify the topic of interest
         ex:isTopicOfCourse ?course.

  # Getting lecture numbers and material types linked to the topic
  ?topic ex:isTopicOfLecture ?lecture;
         ex:materialType ?materialType.

  # Extracting lecture number from the lecture URI
  BIND(REPLACE(STR(?lecture), "http://example.org/vocab/lecture/", "") AS ?lectureNumber)
}

10.
PREFIX ex: <http://example.org/vocab/>

SELECT ?topic
WHERE {
  ?student ex:completedCourse ?completedCourse .
  ?completedCourse ex:course ?course .
  ?course ex:number "6651" .
  ?student ex:hasCompetency ?topic .
}

11.
PREFIX ex: <http://example.org/vocab/>

SELECT ?grade
WHERE {
  ?student ex:completedCourse ?completedCourse .
  ?completedCourse ex:course ?course ;
                   ex:courseGrade ?grade .
  ?course ex:number "6651" .
  FILTER(?student = <http://example.org/vocab/student/S1001>)
}

12.
PREFIX ex: <http://example.org/vocab/>

SELECT ?student
WHERE {
  ?student ex:completedCourse ?completedCourse .
  ?completedCourse ex:course ?course .
  ?course ex:number "6651" .
}

13.
PREFIX ex: <http://example.org/vocab/>

SELECT ?courseNumber ?grade
WHERE {
  ?student ex:completedCourse ?completedCourse .
  ?completedCourse ex:course ?course ;
                   ex:courseGrade ?grade .
  ?course ex:number ?courseNumber .
  FILTER(?student = <http://example.org/vocab/student/S1001>)
}
